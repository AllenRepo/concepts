using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace DesignPattern.Test
{
    [TestClass]
    public class UnitTest1
    {
        /*
            Adapter Pattern
            definition :    design pattern that allows the interface of an existing class to be used from another interface. 
                            It is often used to make existing classes work with others without modifying their source code.
            solves :        abstraction of functionality, development separately
            real world :    american plug => adapter => foreign outlet
            ex :            ADO.NET adapters OleDbAdapater, XmlAdapter
            related : 
            hint :          outlet
        */
        [TestMethod]
        public void TestMethod1()
        {
        }
    }
}
